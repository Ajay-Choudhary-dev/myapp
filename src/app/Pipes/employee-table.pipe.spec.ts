import { EmployeeTablePipe } from './employee-table.pipe';

describe('EmployeeTablePipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeeTablePipe();
    expect(pipe).toBeTruthy();
  });
});
